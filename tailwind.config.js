/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./src/**/*.{js,ts}", // Asegúrate de incluir todos los archivos JS/TS en la carpeta src
    "./index.html"        // Asegúrate de incluir tu archivo HTML principal
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};